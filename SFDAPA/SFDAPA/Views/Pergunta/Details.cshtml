@model Model.Models.Pergunta

@{
    ViewBag.Title = "Details";
}

<h2>Detalhes</h2>

<div>
    <h4>Pergunta</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Questao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Questao)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.Codigo }) |
    @Html.ActionLink("Voltar", "Index", new { id = Model.Assunto.Codigo })
</p>
